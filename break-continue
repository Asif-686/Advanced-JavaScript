const numbers = [1, 3, 2, 5, 9, 4, 8, 7, 6];
const num = numbers.sort((a,b)=>a-b);
for (let i = 0; i < numbers.length; i++) {
    if (numbers[i] > 8) {
        break;
    }
    if (numbers[i] % 2 == 0) {
        continue;
    }
    console.log(numbers[i]);
}
console.log(...num);